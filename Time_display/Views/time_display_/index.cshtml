<!DOCTYPE html>
<html>
    <head>
        <meta charset='utf-8'>
        <title>Hello!</title>
    </head>
    <body>
        <h1>The current time and date:</h1>
        @{
            DateTime CurrentTime = DateTime.Now;
                // We can render HTML from within C# code!
                <h4>@CurrentTime.ToString("MMMM dd, yyyy")</h4>
                <h4>@CurrentTime.ToString("hh:mm tt")</h4>
        }
    </body>
</html>